This is example.info, generated by atdoc on 2021-01-25 11:01:24 from the
following Common Lisp packages: example/app, example/class,
example/utils



File: example.info,  Node: Top,  Next: Package example/app,  Up: (dir)

1 Top
*****

This manual documents the following packages:

example/app: This is docstring for the example/app package. The package
contains a function which does it's job by applying transformation to
the first and second arguments: -- Function: foo (first &key (other
100500)) Arguments: first --- Just a first argument.other --- Optional
keyword argument. Default is 100500.Return Values: A string with first
and other concatenated. Details: This is example function. Internally it
calls example/utils:do-the-job (*Note Package example/utils::) to do the
real job. Note, that the link above is broken, but Coo does not warn us
when building the docs. Sphinx issues a warning inn such case. See also:
* example/utils:do-the-job (*Note Package example/utils::) When you
mention a function like that, it is included into the package
description and removed from the "Other functions..." section

example/class: This package demonstrates how ATDOC displays classes and
generic functions. The key consept is user class: -- Class: user
Superclasses:
common-lisp:standard-objectsb-pcl::slot-objectcommon-lisp:t Documented
Subclasses: example/class:admin (*Note Package
example/class::)example/class:non-documented-user (*Note Package
example/class::) Direct Slots: email --- Correct email address.
last-login-at --- name --- A full username. Details: All users in the
system have this class. Last login slot is updated automatically.NOTE:
"Documented Subclasses" section contains only classes which are: It is
possible to check if user has admin privileges, using this function: --
Function: is-admin (user) Details: Returns t if user can modify the
system. Right now, is-admin (*Note Package example/class::) returns t
only for objects of admin (*Note Package example/class::): -- Class:
admin Superclasses: example/class:user (*Note Package
example/class::)common-lisp:standard-objectsb-pcl::slot-objectcommon-lisp:t
Documented Subclasses: None Direct Slots: None Details: Admins should
have additional priveleges.

example/utils: The utils package.This package's docstring is not mention
any functions via aboutfun or aboutclass tags.Hence, the only exported
function do-the-job (*Note Package example/utils::) will be show in a
separate section "Other functions in example/utils".


* Menu:

* Package example/app::
* Package example/class::
* Package example/utils::



File: example.info,  Node: Package example/app,  Next: Package example/class,  Prev: Top,  Up: Top

1.1 Package example/app
***********************

This is docstring for the example/app package.

The package contains a function which does it's job by applying
transformation to the first and second arguments:



-- Function: foo (first &key (other 100500))

     Arguments:
       first --- Just a first argument.
       other --- Optional keyword argument. Default is 100500.

     Return Values: A string with first and other concatenated.

     Details:

       This is example function.

       Internally it calls example/utils:do-the-job (*Note Package
       example/utils::) to do the real job.

       Note, that the link above is broken, but Coo does not warn us
       when building the docs. Sphinx issues a warning inn such case.

     See also:

       * example/utils:do-the-job (*Note Package example/utils::)

When you mention a function like that, it is included into the package
description and removed from the "Other functions..." section



File: example.info,  Node: Package example/class,  Next: Package example/utils,  Prev: Package example/app,  Up: Top

1.2 Package example/class
*************************

This package demonstrates how ATDOC displays classes and generic
functions.

The key consept is user class:



-- Class: user

     Superclasses:

       common-lisp:standard-object
       sb-pcl::slot-object
       common-lisp:t

     Documented Subclasses:

       example/class:admin (*Note Package example/class::)
       example/class:non-documented-user (*Note Package example/class::)

     Direct Slots: email --- Correct email address. last-login-at ---
     name --- A full username.

     Details:

       All users in the system have this class.

       Last login slot is updated automatically.

       NOTE: "Documented Subclasses" section contains only classes which
       are:

It is possible to check if user has admin privileges, using this
function:



-- Function: is-admin (user)

     Details:

       Returns t if user can modify the system.

Right now, is-admin (*Note Package example/class::) returns t only for
objects of admin (*Note Package example/class::):



-- Class: admin

     Superclasses:

       example/class:user (*Note Package example/class::)
       common-lisp:standard-object
       sb-pcl::slot-object
       common-lisp:t

     Documented Subclasses:

       None

     Direct Slots: None

     Details:

       Admins should have additional priveleges.



File: example.info,  Node: Package example/utils,  Next: Symbol Index,  Prev: Package example/app,  Up: Top

1.3 Package example/utils
*************************

The utils package.

This package's docstring is not mention any functions via aboutfun or
aboutclass tags.

Hence, the only exported function do-the-job (*Note Package
example/utils::) will be show in a separate section "Other functions in
example/utils".



File: example.info,  Node: Symbol Index,  Prev: Top,  Up: (dir)

2 Symbol Index
**************


* Menu:


